version: "3.2"
services:
  site:
    command: jekyll serve -wI
    image: jekyll/jekyll
    volumes:
      - $PWD:/srv/jekyll
      - $PWD/vendor/bundle:/usr/local/bundle
    ports:
      - 4000:4000
